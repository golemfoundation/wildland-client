{
    "title": "Storage manifest (local, cached)",
    "allOf": [
        { "$ref": "storage.schema.json" },
        {
            "type": "object",
            "required": ["path"],
            "properties": {
                "path": {
                    "$ref": "types.json#abs_path",
                    "description": "Path in the local filesystem"
                }
            }
         }
    ]
}
