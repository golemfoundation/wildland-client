{
    "title": "Storage manifest (S3)",
    "type": "object",
    "required": ["signer", "type", "container_path", "bucket"],
    "properties": {
        "signer": { "$ref": "types.json#fingerprint" },
        "type": { "const": "s3" },
        "container_path": { "$ref": "types.json#abs_path" },
        "bucket": {
            "type": "string",
            "description": "S3 bucket name"
        }
    },
    "additionalProperties": false
}
