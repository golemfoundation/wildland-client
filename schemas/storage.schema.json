{
    "title": "Storage manifest (common schema)",
    "type": "object",
    "required": ["signer", "type", "container_path"],
    "properties": {
        "signer": { "$ref": "types.json#fingerprint" },
        "type": { "type": "string" },
        "container_path": { "$ref": "types.json#abs_path" }
    }
}
