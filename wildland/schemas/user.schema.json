{
    "title": "User manifest",
    "type": "object",
    "required": ["signer", "paths"],
    "properties": {
        "signer": { "$ref": "types.json#fingerprint" },
        "paths": {
            "type": "array",
            "items": { "$ref": "types.json#abs-path" },
            "minItems": 1,
            "description": "list of paths for this user"
        },
        "containers": {
            "type": "array",
            "items": { "$ref": "types.json#url" },
            "description": "deprecated alias for infrastructures"
        },
        "infrastructures": {
            "type": "array",
            "items": {
                "oneOf": [
                    { "$ref": "types.json#url" },
                    { "$ref": "container.schema.json" }
                ]
            },
            "description": "list of container URLs (for finding this user's manifests); can also contain inline containers"
        }
    },
    "additionalProperties": false
}
